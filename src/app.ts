import { timers } from "jquery";
import { Canvas } from "./canvas";
import { firestreg } from "./firestreg";
import { mandelbrot } from "./mandelbrot";
import { Timer } from "./timer";
import { trekant } from "./trekant";
import { randInt, random, slider, slider2 } from "./utils";





export const setup = (c: Canvas) => {
    // runs once at start
    
    
    
}

export const loop = (c: Canvas, delta: number) => {
    // runs continueally after setup
    
    
    
    
    
    
}
